{
  "name": "gocoffee",
  "version": "1.0.0",
  "description": "This is goCOFFEE project backend",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "build": "tsc",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "OTAJON QUTBIDDINOV",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
